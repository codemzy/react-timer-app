var React = require('react');

var Timer = (props) => {
    return (
        <div>
            <h1 className="text-center page-title">This is the Timer.</h1>
        </div>
    ); 
};

module.exports = Timer;