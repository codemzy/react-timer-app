var React = require('react');

var Countdown = (props) => {
    return (
        <div>
            <h1 className="text-center page-title">This is the Countdown.</h1>
        </div>
    ); 
};

module.exports = Countdown;